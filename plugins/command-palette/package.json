{
  "name": "bb-plugin-command-palette",
  "version": "0.1.0",
  "type": "module",
  "description": "Thread actions for bb's quick palette: pin, archive, settle, and snooze the thread in view.",
  "engines": {
    "bb": ">=0.40",
    "bbPluginSdk": ">=0.4.21"
  },
  "bb": {
    "name": "Command Palette",
    "description": "Thread actions for bb's quick palette: pin, archive, settle, and snooze the thread in view.",
    "branding": {
      "icon": "Search"
    },
    "server": "./server.ts",
    "app": "./app.ts",
    "skills": []
  },
  "keywords": [
    "bb-plugin"
  ],
  "scripts": {
    "build": "bb plugin build",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "0.4.21",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "better-sqlite3": "^12.0.0",
    "cron-parser": "^5.10.0",
    "hono": "^4.11.9",
    "sonner": "^2.0.8",
    "typescript": "^5.7.0",
    "vitest": "^4.1.11"
  }
}
